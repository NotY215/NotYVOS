# NOTYVOS Limine boot menu — Phase 0
# Note: Limine v8.x uses limine.conf (replaces the older limine.cfg).

timeout: 0
verbose: yes
interface: text

/NOTYVOS (Phase 0)
    protocol: limine
    kernel_path: boot():/boot/notyvos-kernel.elf